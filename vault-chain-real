git clone https://github.com/all010/vault-chain-real.git
cd vault-chain-real

mkdir -p {node,pallets/medical-records/src,mobile,docs,scripts}
touch README.md docs/whitepaper.md .gitignore

# Vault Chain Real

> مشروع مفتوح المصدر لبناء نظام تشفير بلوكشيني لملفات الصحة – خطوة بخطوة، قابل للتشغيل.

## الحالة الحالية
- ✅ مستودع جديد
- 🔄 قيد بناء الـ MVP
- ❌ غير جاهز للإنتاج

## التكنولوجيا المستخدمة
| المكون | التكنولوجيا |
|--------|-------------|
| سلسلة بلوك | Substrate (Rust) |
| تشفير البيانات | AES-256-GCM |
| تخزين خارج السلسلة | IPFS |
| تطبيق جوال | Flutter |

## كيف تشغل المشروع محليًا؟
سيتم توثيق كل خطوة في ملف `docs/SETUP.md`.

## كيف تساهم؟
افتح issue أو اسحب PR – كل مساهمة مرحب بها.

git add .
git commit -m "Initial structure: Vault Chain MVP"
git push origin main

